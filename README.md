## Edit & publish

1. Edit `.md` files, commit, push to `main`.
2. GitHub builds HTML automatically. No local build required.

Optional local preview: `bundle install` + `bundle exec jekyll serve` (needs Ruby; add a `Gemfile` with `github-pages` gem first).

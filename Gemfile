source "https://rubygems.org"

# github-pages pins Jekyll 3.9, which breaks on Ruby 4.0 locally.
# Production GitHub Pages still builds with its own stack; this Gemfile is for local preview only.
gem "jekyll", "~> 4.3"
gem "webrick", "~> 1.8"
